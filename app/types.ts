export type Post = {
    id: string;
    title: string;
    author: string;
    category: string;
    publishedDate: string;
};

export type Student = {
    id: string;
    name: string;
    grade: string;
    age: number;
    major: string;
    enrollmentDate: string;
};